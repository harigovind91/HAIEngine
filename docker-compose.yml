version: '3.8'
services:
  backend:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    volumes: ["./db:/app/db"]
  frontend:
    build: .
    command: streamlit run frontend/app.py --server.port 8501
    ports: ["8501:8501"]
    depends_on: [backend]
    
